create schema bdd_nutriRecetas;
use bdd_nutriRecetas;

	CREATE TABLE _user(
	user_id INT AUTO_INCREMENT PRIMARY KEY,
    user_name VARCHAR(45) NOT NULL,
    user_lastname VARCHAR(45),
    passwrd VARCHAR(100) NOT NULL,
	email VARCHAR(100) UNIQUE NOT NULL
);

INSERT INTO _user VALUES(
	0,
    "Emilia",
    "Perez",
    "con",
    "correo"
);

-- QUITAR COLUMNA user_name y user_lastname PORQUE NO SE OCUPAN
ALTER TABLE _user
DROP COLUMN user_name,
DROP COLUMN user_lastname;

-- ADICIONES PARA LA AUTENTIFICACION DE USUARIOS
ALTER TABLE _user
ADD COLUMN is_verfied TINYINT(1) DEFAULT 0,
ADD COLUMN verification_code VARCHAR(6) DEFAULT NULL;

-- AQUI ME EQUIVOQUE xd
ALTER TABLE _user CHANGE is_verfied is_verified TINYINT(1);

-- SOLO EN CASO DE USAR OTRO USUARIO QUE NO SEA ROOT PARA EJECUTAR LA APLICACION EN ANDROID STUDIO
-- DESDE EL USUARIO ROOT DAR PRIVILEGIOS
GRANT ALL PRIVILEGES ON bdd_nutrirecetas.* TO 'ignacio'@'%';

-- SE APLICAN LOS CAMBIOS
FLUSH PRIVILEGES;